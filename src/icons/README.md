# `src/icons`

Logos de marca a color. Pendiente — **Fase 5**.

- `brand.ts` — mapeo dominio/nombre → logo a color (colección "Logos") desde
  `assets/brand-logos/`, render con `react-native-svg`; fallback a avatar inicial + color.

Iconos de UI: usar `@expo/vector-icons` (NO `react-icons`). Ver
`docs/adr/0004-iconos-logos-color.md`.
